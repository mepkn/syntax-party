No truthiness — write the comparison you mean.

```swift
let ok = !s.isEmpty          // non-empty string
let positive = n != 0        // non-zero number
let exists = opt != nil      // Optional holds a value
```
