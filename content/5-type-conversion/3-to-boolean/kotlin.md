No truthiness — write the comparison you mean.

```kotlin
val ok = s.isNotEmpty()      // non-empty string
val positive = n != 0        // non-zero number
val exists = user != null    // non-null reference
```
