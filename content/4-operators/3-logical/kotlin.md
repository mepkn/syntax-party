Operands must be `Boolean`; the elvis operator `?:` handles null fallbacks.

```kotlin
true && false      // false
true || false      // true
!true              // false

name ?: "default"  // elvis — fallback when null
```
