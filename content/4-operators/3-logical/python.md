`and` / `or` return one of their operands, not a boolean.

```python
True and False    # False
True or False     # True
not True          # False

"" or "fallback"  # "fallback" — common default idiom
```
