`const` enforces immutability at compile time; only primitive types allowed.

```go
const PI = 3.14159
const MaxRetries = 3

// grouped constants
const (
    StatusOK       = 200
    StatusNotFound = 404
)
```
