Use `var` or the short `:=` declaration (infers type); `:=` is preferred inside functions.

```go
var name string = "John"
var age int = 25

// short declaration — infers type
name := "John"
age := 25
isActive := true
```
