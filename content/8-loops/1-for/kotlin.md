`..` is an inclusive range; `until` excludes the upper bound.

```kotlin
for (i in 0 until 5) println(i)   // or 0..4

for (x in listOf("a", "b")) println(x)

for ((k, v) in obj) println("$k $v")
```
