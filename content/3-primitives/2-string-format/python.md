f-strings (prefix `f`) with `{}` expressions. Recommended since Python 3.6.

```python
name = "Alice"
age  = 30

greeting = f"Hello, {name}! You are {age} years old."
expr     = f"{2 + 2} is four"
padded   = f"{name:>10}"   # right-align in a 10-character field
```
