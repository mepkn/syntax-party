Template literals with `${}` — any expression is allowed inside the braces.

```js
const name = "Alice"
const age = 30

const greeting = `Hello, ${name}! You are ${age} years old.`
const expr = `${2 + 2} is four`
```
