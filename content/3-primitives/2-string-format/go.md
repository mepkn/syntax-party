`fmt.Sprintf` with printf-style verbs — returns a formatted string without printing.

```go
name := "Alice"
age  := 30

greeting := fmt.Sprintf("Hello, %s! You are %d years old.", name, age)
pi       := fmt.Sprintf("Pi is approximately %.2f", 3.14159)
```
