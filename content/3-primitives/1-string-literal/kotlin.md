Single quotes are `Char`; triple quotes are raw strings — no escapes needed.

```kotlin
val single = 'h'              // Char, not String
val double = "hello"
val multiline = """line one
line two"""
```
