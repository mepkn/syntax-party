Only double quotes — a `Character` needs an explicit type annotation.

```swift
let char: Character = "h"     // no single-quote syntax
let double = "hello"
let multiline = """
line one
line two
"""
```
