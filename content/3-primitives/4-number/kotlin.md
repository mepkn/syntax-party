`Int` is 32-bit, `Long` is 64 — integer overflow wraps silently.

```kotlin
val integer = 42              // Int
val long = 42L                // Long
val float = 3.14              // Double
val scientific = 1.5e10
val readable = 1_000_000      // underscores as separators
```
